function c(t) {
  const n = window.scrollY ?? 0, o = window.scrollX ?? 0, e = t.getBoundingClientRect();
  return {
    top: e.top + n,
    left: e.left + o
  };
}
function l(t) {
  const n = t.getBoundingClientRect(), o = c(t);
  return {
    top: +o.top + Math.round(n.height / 2),
    left: +o.left + Math.round(n.width / 2)
  };
}
export {
  l as c,
  c as g
};
