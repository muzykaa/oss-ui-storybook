<template>
  <div :class="{ 'top-nav-bar__link': true }">
    <div :class="['top-nav-bar__dropdown', { 'top-nav-bar__dropdown--open': isDropdownOpen }]" @click="toggleDropdown">
      <div>'Create RFP'</div>
      <!--      <SvgImage name="angle-dropdown" class="top-nav-bar__dropdown-icon" />-->
    </div>
    <div v-if="isDropdownOpen" class="dropdown-content">
      <a class="active" :class="{ 'dropdown-item': true }" href="#" @click="closeDropdown"> Web Build </a>
      <a :class="{ 'dropdown-item': true }" href="#" @click="closeDropdown"> Templates </a>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue';

const isDropdownOpen = ref(false);

const toggleDropdown = () => {
  isDropdownOpen.value = !isDropdownOpen.value;
};

const closeDropdown = () => {
  isDropdownOpen.value = false;
};
</script>

<style scoped lang="scss">
.dropdown-content {
  position: absolute;
  top: 30px;
  left: -10px;
  display: flex;
  flex-direction: column;
  background-color: #fff;
  min-width: 120px;
  box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 20%);
  z-index: 1;
}

.dropdown-content .dropdown-item {
  padding: 12px 16px;
  text-decoration: none;
  display: block;
  text-align: left;
  color: #0000c9;
}

.dropdown-content .dropdown-item:hover {
  background-color: #ececec;
}
</style>
