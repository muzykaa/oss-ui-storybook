import { mount } from '@vue/test-utils';
import ServicesMenu from './ServicesMenu.vue';
import { AlertType } from '@/enum/AlertType';
import { describe } from 'vitest';

// TODO add tests
describe('Test for ServicesMenu component', () => {
  test('Alert component', async () => {
    expect(ServicesMenu).toBeTruthy();

    // const wrapper = mount(ServicesMenu, {
    //   props: {
    //     id: 'alert',
    //     content: 'content',
    //     type: AlertType.SUCCESS,
    //     ttl: 999,
    //   },
    // });
  });
});
