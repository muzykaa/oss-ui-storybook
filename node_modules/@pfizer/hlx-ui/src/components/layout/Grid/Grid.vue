<template>
  <div class="grid">
    <slot />
  </div>
</template>

<script lang="ts" setup>
interface Props {
  cols: number; // used for desktop
  mCols?: number; // used for mobile devices
}

defineProps<Props>();
</script>

<style lang="scss" scoped>
@use '../../../sass/variables/box-model';

.grid {
  display: grid;
  gap: box-model.$spacing-main;
  grid-template-columns: repeat(v-bind('mCols'), 1fr);

  @media (min-width: 991px) {
    grid-template-columns: repeat(v-bind('cols'), 1fr);
  }
}
</style>
