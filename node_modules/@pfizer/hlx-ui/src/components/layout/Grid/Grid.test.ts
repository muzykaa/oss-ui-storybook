import { mount } from '@vue/test-utils';
import Grid from './Grid.vue';
import { describe } from 'vitest';

// TODO add tests
describe('Test for Grid component', () => {
  test('Grid component', async () => {
    expect(Grid).toBeTruthy();

    // const wrapper = mount(Grid, {
    //   props: {
    //     cols: 3,
    //   },
    // });
  });
});
