<template>
  <div :class="['page-header', className, 'container', { 'without-back': !backLink?.length }]">
    <a v-if="backLink?.length" class="button invert page-header__back-btn" :href="backLink">
      <Icon name="arrow-left" />
      {{ 'Back' }}
    </a>

    <h3 class="page-header__title">
      {{ title }}
    </h3>

    <div class="page-header__buttons">
      <slot />
    </div>
  </div>
</template>

<script lang="ts" setup>
import Icon from '../../viewers/Icon/Icon.vue';

interface Props {
  title: string;
  backLink?: string;
  className?: string;
}

defineProps<Props>();
</script>
