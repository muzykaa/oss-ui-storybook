import EditorField from './EditorField.vue';
import { describe } from 'vitest';

describe('Test for EditorField component', () => {
  test('EditorField component', async () => {
    expect(EditorField).toBeTruthy();
  });
});
