import { mount } from '@vue/test-utils';
import StatusTag from './StatusTag.vue';
import { describe } from 'vitest';
import type { StatusVariant } from '@/enum/StatusVariant';

// TODO add tests
describe('Test for StatusTag component', () => {
  test('StatusTag component', async () => {
    expect(StatusTag).toBeTruthy();

    // const wrapper = mount(StatusTag, {
    //   props: {
    //     label: 'status',
    //     variant: 'success' as StatusVariant,
    //   },
    // });
  });
});
