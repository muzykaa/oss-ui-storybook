import { mount } from '@vue/test-utils';
import Menu from './Menu.vue';
import { describe } from 'vitest';

// TODO add tests
describe('Test for Menu component', () => {
  test('Menu component', async () => {
    expect(Menu).toBeTruthy();

    // const wrapper = mount(Menu, {
    //   props: {
    //     items: [],
    //   },
    // });
  });
});
