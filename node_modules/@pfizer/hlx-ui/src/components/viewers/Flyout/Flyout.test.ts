import { mount } from '@vue/test-utils';
import Flyout from './Flyout.vue';
import { describe } from 'vitest';

// TODO add tests
describe('Test for Flyout component', () => {
  test('Flyout component', async () => {
    expect(Flyout).toBeTruthy();

    // const wrapper = mount(Flyout, {
    //   props: {
    //     modelValue: true,
    //     position: {
    //       left: 0,
    //       top: 0,
    //     },
    //   },
    // });
  });
});
