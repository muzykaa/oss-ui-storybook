import { mount } from '@vue/test-utils';
import Popup from './Popup.vue';
import { describe } from 'vitest';

// TODO add tests
describe('Test for Popup component', () => {
  test('Popup component', async () => {
    expect(Popup).toBeTruthy();

    // const wrapper = mount(Popup, {
    //   props: {
    //     modelValue: true,
    //   },
    // });
  });
});
