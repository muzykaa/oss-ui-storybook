<template>
  <component v-bind="{ ...$props, ...mappedProps }" :is="provider.navConfig.link.component">
    <slot />
  </component>
</template>

<script setup lang="ts">
import { useNavigation } from '../../../providers/navigation';

interface Props {
  url: string;
}

const props = defineProps<Props>();
const provider = useNavigation();
const mappedProps = {
  [provider.navConfig.link.propsMap.url]: props.url,
};
</script>
