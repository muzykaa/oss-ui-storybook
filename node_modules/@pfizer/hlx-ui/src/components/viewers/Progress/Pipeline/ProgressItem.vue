<template>
  <div :class="`progress-pipeline__item ${item.isActive ? 'active' : ''}`" @click="itemClickHandler">
    <Icon name="round-check" />
    <div class="progress-pipeline__item-text">
      {{ item.title }}
    </div>
  </div>
</template>

<script lang="ts" setup>
import Icon from '../../Icon/Icon.vue';
import type { Nav } from '../../../../type/etc/nav';

interface Props {
  item: Nav;
}

const props = defineProps<Props>();
const emit = defineEmits(['click']);

const itemClickHandler = () => emit('click', props.item);
</script>
