import type { Preview } from "@storybook/vue3";

import '../src/sass/prototypes.scss';
import 'oss-ui-icons/style.css';

const preview: Preview = {
  parameters: {
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/i,
      },
    },
  },
};

export default preview;
